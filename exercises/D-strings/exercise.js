// Start by creating a variable `message`

let message = "string example";
let messageType = typeof message;

let consoleMessage = function () {
  console.log(message);
  console.log(messageType);
};

consoleMessage();
