function halve(number) {
  // complete the function here
    var half = number / 2;
    return half;
}

var result = halve(12);
var result2 = halve(200);
console.log("First Output: " + result + "\nSecond Output:"+ (result2) );
