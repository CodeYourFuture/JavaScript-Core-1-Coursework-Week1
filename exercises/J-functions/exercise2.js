function triple(number) {
    // complete function here
    return number * 3;
}

var result = triple(12);

console.log(result);

// 1. the word "number" is parameter
// 2. we created a variable then
// 3. the we call the function to pass argument or value
// 4. we console the to see the output