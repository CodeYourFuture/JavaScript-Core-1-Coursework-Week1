//assign 3 variables
var numberOfStudents = 15;
var numberOfMentors = 8;
var total = 23;
//calculate the percentage of students, using the rounding function to round the decimale number.
var studentPercentage = Math.round(numberOfStudents/total*100);
//calculate the percentage of Mentors,using the rounding function to round the decimale number.
var mentorsPercentage = Math.round (numberOfMentors/total*100);
//the results
console.log(studentPercentage);
console.log(mentorsPercentage);
