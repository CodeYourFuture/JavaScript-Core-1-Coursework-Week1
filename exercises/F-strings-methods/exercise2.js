const name = " Mohammad         ";
const trimName = name.trim()
const nameLength = name.length
const message = `My name is ${trimName} and my name is ${nameLength} charecters long`
console.log(message);
