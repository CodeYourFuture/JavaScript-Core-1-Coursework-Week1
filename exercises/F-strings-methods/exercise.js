// Start by creating a variable `message`

let greeting = "Hello, my name is ";
let myName = "Marcus ";
let length = myName.length;
let finisher = `and i have ${length} letters in my name`;
let message = greeting + myName + finisher;

console.log(message);
