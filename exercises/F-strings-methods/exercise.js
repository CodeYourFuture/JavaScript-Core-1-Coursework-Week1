// Start by creating a variable `message`

const name = "Shimen";
const nameLength = name.length;
const message = "My name is " + name + " and the length of my name " + nameLength;
console.log(message);
