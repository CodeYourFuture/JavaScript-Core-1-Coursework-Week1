// Start by creating a variable `message`
const myName = "Oksana";
const message = "My name is";
const messageLowerCase = message.toLocaleLowerCase();
console.log(
  `${message} ${myName} and ${messageLowerCase} ${myName.length} characters long.`
);
console.log(myName.toUpperCase());
