// Start by creating a variable `message`
const yourName = "Daniel";
const nameLength = yourName.length;
const nameLowerCase = yourName.toLowerCase();
console.log(
  "My name is ".concat(
    nameLowerCase,
    " and is ",
    nameLength,
    " characters long."
  )
);
