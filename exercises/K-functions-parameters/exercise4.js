// Declare your function first
function sum(a, b) {
    return a + b;
}
// Call the function and assign to a variable `sum`
var total = sum(13, 124)
console.log(total);

//1. we created a fucntion called "sum"
//2. we passed two parameter "a" and "b"
//3. and created return statment with the addition of paramenters
//4. the created a variable "total"
//5. and then passed arguments to a function