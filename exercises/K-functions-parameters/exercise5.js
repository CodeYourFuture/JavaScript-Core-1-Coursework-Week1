// Declare your function here
function createLongGreeting(name, age) {
  return "Hello, my name is " + name + " and I'm " + age + " years old";
}

const greeting = createLongGreeting("Simon", 36);

console.log(greeting);
