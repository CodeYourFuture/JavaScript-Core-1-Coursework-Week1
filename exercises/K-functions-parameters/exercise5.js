// Declare your function here
function createLongGreeting(myName, myAge) {
    return `My names is ${myName} and I'm ${myAge} years old `
}

const greeting = createLongGreeting("Daniel", 30);

console.log(greeting);