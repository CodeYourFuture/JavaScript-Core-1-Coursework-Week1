// Declare your function here
function createLongGreeting(yourName, yourAge) {
  return `Hello, my name is ${yourName} and I'm ${yourAge} years old`
}
const greeting = createLongGreeting('Daniel', 30)

console.log(greeting)
