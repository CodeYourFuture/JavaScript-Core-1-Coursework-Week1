// Declare your function here
function createLongGreeting(firstName, number) {

    return `Hello, my name is ${firstName} and I'm ${number} years old`
};

const greeting = createLongGreeting("Daniel", 30);

console.log(greeting);
