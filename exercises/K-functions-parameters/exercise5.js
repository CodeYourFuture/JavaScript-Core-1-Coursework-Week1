// Declare your function here
const createLongGreeting = (firstName, age) => {
    return `Hello my name is ${firstName} and I am ${age} years old.`
}
const greeting = createLongGreeting("Daniel", 30);

console.log(greeting);
