// Declare your function first

function divide(b, c) {
    return b / c;
}

var result = divide(3, 4);
console.log(result);

//Here we created a function from scratch and passed two parameters b and c.
//we created return statement that divides b-parameter to c-parameter.
// then we created "result" variable and passed 3 and 4 as the actual arguments that replaced the parameters.