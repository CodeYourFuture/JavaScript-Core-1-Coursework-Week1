const numberOfStudents = 15;
const studentsMessage = `Number of students: ${numberOfStudents}`;
const numberOfMentors = 8;
const mentorsMessage = `Number of mentors: ${numberOfMentors}`;
const total = numberOfStudents + numberOfMentors;
const totalMessage = `Total number of students and mentors: ${total}`;
console.log(studentsMessage);
console.log(mentorsMessage);
console.log(totalMessage);