// Start by creating a variables `numberOfStudents` and `numberOfMentors`
const numberOfStudents = 35;
let messageStudents = "Number of students: " + numberOfStudents;
console.log(messageStudents);

const numberOfMentors = 10;
let messageMentors = "Number of mentors: " + numberOfMentors;
console.log(messageMentors);

let sumStudentsAndMentors =
  "Total number of students and mentors: " +
  (numberOfStudents + numberOfMentors);
console.log(sumStudentsAndMentors);
