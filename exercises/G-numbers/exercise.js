// Start by creating a variables `numberOfStudents` and `numberOfMentors`

let numberOfStudents = 15;
let numberOfMentors = 8;

let sumOfStudentsAndMentors = numberOfStudents + numberOfMentors;

let studentMessage = `number of students: ${numberOfStudents}.`;
let mentorMessage = `number of mentors: ${numberOfMentors}.`;
let groupTotal = `Total numnber of students and mentors: ${sumOfStudentsAndMentors}.`;
let groupTotal2 = `Total numnber of students and mentors: ${
  numberOfMentors + numberOfStudents
}.`;

console.log(studentMessage);
console.log(mentorMessage);
console.log(groupTotal);
console.log(groupTotal2);
