// Start by creating a variables `numberOfStudents` and `numberOfMentors`
let numberOfStudents = 15;
let numberOfMentors = 8;
const total = numberOfStudents + numberOfMentors;
let studentMessage = `number of students: ${numberOfStudents}`;
let mentorsMessage = `number of mentors: ${numberOfMentors}`;
let totalMessage = `total number of students and mentors ${total}`;

console.log(studentMessage);
console.log(mentorsMessage);
console.log(totalMessage);
